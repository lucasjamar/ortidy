# Binder environment for the example notebooks.
# Pin the minimum version so Binder rebuilds its cached image on a new release
# (the notebooks use the long-form API introduced in 0.4.0).
ortidy>=0.4.0
pandas
plotly
